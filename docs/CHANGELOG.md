# 变更记录

格式遵循 Keep a Changelog；版本号遵循语义化版本。

## [未发布] — 0.6.0 之后

### 验证（①真实剪贴板回读刷新到五批后终树）
- 渲染器五批改动全部落地后重跑 `qa:clipboard`：**6/6 通过**（复制富文本落盘 HTML 自带 data:image、内联样式在位、无 style 块、无「图片未随带」；复制此图与原素材逐字节一致）——①的 exe 级证据不再停留在 0.6.0 时期，与当前代码树一致；跑前备份/跑后恢复用户剪贴板并验证删除

### 文档（手册追平对标五批实况）
- USER-GUIDE 写作页条目新增**写法速查**（加粗/高亮/删除线/行内代码/待办 ☐☑/表格 ⊞ 一键插入/引用式链接/自动链接/脚注，并讲清「外链自动变角注+文末参考区」的微信适配逻辑）；示例文章演示范围表述订正为含表格与待办清单；README 目录图 md2wechat 行同步
- RESEARCH-对标调研.md 补「**Markdown 元素覆盖面**」对照维度（doocs/mdnice 基本预期 vs 五批闭环实况，判定对齐+部分超越）、硬伤清单第 6 条与首启行演示范围同步

### 新增（⑤对标第五批：写作页表格一键插入）
- 表格渲染支持后**插入通道补齐**：工具栏新增「⊞ 表格」按钮，一键插入三列表头+对齐行+示例行骨架，且**自动选中首格「列A」直接打字换表头**（同「图槽」按钮的即插即改肌肉记忆）；不会 markdown 语法的 135 类用户从此不用手敲竖线——doocs 等纯编辑器无此引导，属超越项
- 门禁：test-ux 23→25 项（按钮接线/骨架含对齐行+自动选区），`npm test` 全链 209→211 项，lint 34 全过；终树 `npm run verify` 真实 AI 链路 21/21 通过
- exe 实测留证（00:45 重打包 exe，win-unpacked 内含按钮与骨架代码）：全新 profile GJ_SHOT 人审——工具栏 ↗ 旁 ⊞ 真实显形，表格/☑☐/角注[1]/图槽渲染如旧无回归，LEFTOVERS=0

### 修复（⑤对标补漏：GFM 脚注，杜绝定义行静默吞掉）
- **第三批改造预扫时埋下的回归被清掉**：`[^1]: 脚注内容` 定义行会被引用式链接预扫误吞（`[^1]` 恰好命中「方括号+冒号+URL」形态）——脚注内容**凭空消失**、正文只留字面 `[^1]`，属静默丢内容硬伤（违反"不静默失败"红线）。现脚注定义行**先行收集剥除**，`[^id]` 标记接入与链接角注**同一套文末连续编号**（白底主题色上标，脚注内容直排编号下），未定义标记照旧保留字面不误吞；单行定义即覆盖绝大多数写法，多行续行暂按普通段落
- 门禁：test-richcopy 29→32 项（脚注上标+文末内容+不漏字面/与链接角注连续编号/未定义保留字面），`npm test` 全链 206→209 项，lint 34 全过；终树 `npm run verify` 真实 AI 链路 21/21 通过
- exe 实测留证（00:36 重打包 exe，win-unpacked 源码 grep fnDefs 3 处命中确认在包内）：全新 profile GJ_SHOT 回归人审——表格/☑☐/角注[1]/图槽照常真实渲染无回归，LEFTOVERS=0

### 新增（⑤对标补漏：引用式链接与自动链接）
- `[文字][id]` 引用式链接与 `<https://…>`/`<www.…>` 自动链接接入**同一套角注机制**（微信正文外链不可点，定义行 `[id]: url` 从正文剥除、汇总进文末参考区）；`[别名][]` 隐式引用按别名回查；**未定义的引用保留字面不误吞**（宁缺勿错，与你写的意图一致）
- 门禁：test-richcopy 26→29 项（引用式变角注+定义行剥除/自动链接可见 URL+角注/未定义引用不误吞），`npm test` 全链 203→206 项，lint 34 全过；终树 `npm run verify` 真实 AI 链路 21/21 通过
- exe 实测留证（00:19 重打包 exe，win-unpacked 源码 grep 引用式代码 2 处命中）：全新 profile GJ_SHOT 回归人审——表格/☑☐ 待办/链接角注[1]在预览照常真实渲染，601 字示例无回归

### 新增（⑤对标补漏：Markdown 表格渲染）
- **GFM 表格从乱码竖线变为正式排版**：此前写作页/复制富文本把 `| 列A | 列B |` 原样渲染成含转义竖线的段落，粘贴后台即乱码——doocs/mdnice 等同类均支持表格，属对标硬伤。现按表头行+分隔行精确识别（含竖线的普通文字如「价格 1|2 元」不误伤），渲染为**全内联样式** `<table>`（微信后台兼容，无 `<style>` 块）：分隔行 `:--- / :---: / ---:` 控制列左/中/右对齐，偶数行斑马纹，单元格内加粗、行内代码、图片照常生效；列数按表头归一（缺列留空、多列截齐），横向滚动容器兜窄屏长表
- 首启示例文章新增表格演示（「写作页能演示的元素都装在这里」自此含表格一档）
- **顺带清掉两处同类字面泄漏**：`~~删除线~~` 渲染为 `<del>`（不再露波浪线）、`- [ ] / - [x]` 任务清单渲染为 ☐/☑ 前缀（微信正文无 checkbox 控件，用主题色字符呈现）；两者在表格单元格内同样生效，示例文章新增「待办清单」一节演示
- **审计出的三处存量渲染缺陷一并修复**：多行段落软换行此前会露出字面 `<br>` 文本（inline() 把 join 分隔符也转义了——AI 成稿常见多行段落，属真实生产缺陷），改为逐行 inline 后拼接；`#####/######` 五六级标题不再漏出井号字面（并入 h3 样式）；`> >` 嵌套引用剥掉多余尖括号
- 门禁：test-richcopy 15→26 项（表格产出 `<table>`/样式全内联无 style 块/三列对齐/单元格加粗/普通竖线不误判/删除线/勾选符号/普通列表项不误加勾选/软换行真实 `<br>`/h5 不留井号/嵌套引用剥尖括号），test-ux 示例演示断言同步；`npm test` 全链 192→203 项，lint 34 全过；终树 `npm run verify` 真实 AI 链路 21/21 通过
- exe 实测留证（重打包后新 exe）：GJ_SHOT 新增 `GJ_SHOT_SCROLL` 钩子（截图前注入一段 JS 滚动到目标元素，仅留证用），全新 profile 首启示例文章真实渲染——表头底色、`:---:` 列居中、单元格内加粗/高亮、斑马纹、「待办清单」☑/☐ 前缀在预览逐一体现（00:11 终版 exe，601 字示例含全部新元素）

### 稳定性（20 分钟真实驻留）
- **0.6.0 终版 exe（含限流重试）全新 profile 驻留 20 分钟**：托盘常驻零操作，全进程树（引导+渲染+GPU 逐代 BFS）内存四点采样 424.7 → 412.1 → 409.3 → 409.3 MB——全程存活、零崩溃、曲线只降不升（此前最长驻留记录仅 5.5 分钟）；收尾脚本按自启进程树精确清理，leftovers=0，不误伤用户任何其他进程

### 文档体系（六本全部追平 0.6.0 实况）
- **新增 [RESEARCH-对标调研.md](RESEARCH-对标调研.md)**：mdnice/墨滴/doocs/135/新榜等 8 维度对照表（带图复制/生图门槛/无权限账号/数据主权/定时发布/首启/AI 边界），超越·追赶·独有逐项判定，并映射 ①~⑤ 硬伤闭环——调研结论自此在案可查
- DECISIONS：新增 ADR-011~014（复制富文本转正 supersede 降级定位 / 免费直连默认源 / 备份包密钥双向硬拒 / 定时提醒模式与错过补救）
- PRD 升 v0.2：①~⑤ 需求增量索引；ARCHITECTURE 分层图补 imagegen/backup/core-loader 模块与 UI 9 脚本；UI-DESIGN 四页控件表、HITL 定律 4（提醒模式分支）、主题清单 3→8 套对齐；DEVELOPMENT 补 `npm test`（8 套 192 项）行
- **首启 SmartScreen 自救入册（⑤首启补漏）**：手册通篇原假设"软件已打开"，而未签名 exe 首跑必遇「Windows 已保护你的电脑」蓝窗——新增第一节「第 0 步」（免安装双击+更多信息→仍要运行+数据落点指路设置页按钮）与排查表首行；README 已知边界同步指路。数据落点经实测核实为 `%APPDATA%\gaojiang`，手册不写死路径、以「打开数据目录」按钮为准（UI→IPC→shell.openPath 三层线追踪兑现）

### 改善（②免费出图高峰体验）
- **免费直连源限流自动重试一次**：实测撞上游共享池高峰 429→500（正文内嵌上游限流详情），此前把原始 JSON 直接甩给用户。现在识别限流特征（429/5xx/正文含 429·per-user limit）自动换 seed 重试一次；仍堵则人话显形「免费档这会儿拥挤…等 1-2 分钟再点『AI 出图』；急用切自带 Key 源」并附截断详情。非限流错误（401/404 等）不重试、保持原可操作文案
- 免费档如实限制从两条公示为三条（水印/中文长提示词/**高峰拥堵+自动重试承接**），keyHint、设置页免 Key 提示、USER-GUIDE 三处同步
- 门禁：test-imagegen 48→51 项（重试成功换 seed、双堵人话文案、非限流不重试），`npm test` 全链 192 项；真实端点 E2E 复验 49.2s 出图成功（新路径实战走通，水印/中文跑偏两条已公示限制依然如实）
- exe 实测留证（重打包后新 exe）：全新 profile 零凭证 `demo-imgtest` 真实点击「测试出图」→「✓ 3205ms，出图 46KB（jpg）→ 可以到配图页一键出图了」，设置页免 Key 提示同步显形第三条限制「高峰拥堵会自动重试一次（仍堵会明说）」；同树 `npm run verify` 真实 AI 链路 21/21 通过

## [0.6.0] - 2026-09-21 — 对标同类工具：带图复制、一键出图、无API死角、备份迁移、体验超越

### 新增（①带图复制闭环）
- **「复制富文本」自带本地图**：正文引用的素材图自动转 dataURL 内联进剪贴板 HTML，粘贴公众号后台图片直接显示；素材缺失不再静默丢图，显形「图片未随带」并列出缺哪张
- **配图页逐张「复制此图」**：每张已成图以位图写入剪贴板，公众号后台图片位置直接 Ctrl+V 即上传——内联失败或图太大时的逐张补图兜底通道
- 门禁：`test-richcopy.mjs` 15 项断言 + `qa:clipboard` 真实剪贴板回读留证 6 项（自备留证数据、跑完即清理）
- exe 实测留证（0.6.0 终版打包·①全链 7/7）：全新 profile 零凭证下 `demo-copyrich` 真实点击 → 落盘 HTML 自带 data:image、内联样式在位、无 `<style>` 块、无「图片未随带」；`demo-copyimg` 真实点击 → 剪贴板位图与素材库原图逐字节一致（钩子改轮询等待，防打包 exe 慢启动假失败）

### 新增（②配图一键出图）
- **「免费直连（免Key）」生图源设为默认**：内置零配置免费端点（GET 直返图片字节，文件头魔数嗅探防伪 200 错误页，flux 池避开默认后端共享限流），装好即用、不填任何 Key 即可一键出图——对标同类"必须注册换 Key 才能生图"的门槛。免费档两条限制如实显形（右下角带源站水印、中文长提示词偏弱建议简短英文），正式配图仍可切自带 Key 的源
- **设置页「生图服务」卡片**：内置国内可达的文生图预设——智谱 CogView（cogview-3-flash 免费档）/ 硅基流动 FLUX.1-schnell / 自定义 OpenAI 兼容端点；模型、Base URL、尺寸随预设联动，「测试出图」不保存也能真实生成一张验证连通
- **配图页「AI 出图」按钮**：图槽卡片上一键出图（提示词可先「AI 生成提示词」再手改）；候选图先显示在卡片里标「待你确认」，点「用这张落正文」才写入文章（可撤销），「弃用」不污染正文——生图结果永远经人工确认落位（HITL）
- 生成的图片自动落素材库（`ai-时间戳` 命名），随「复制富文本」内联带图、发布时自动转微信 CDN，与本地选图同权
- 生图 Key 经 Windows DPAPI 加密只存本机（与微信 AppSecret 同等待遇），支持 `GJ_IMG_KEY` 环境变量引导；未填 Key 时报错直接指路「设置 → 生图服务」
- 测试门禁：新增 `test-imagegen.mjs` 48 项断言（假 fetch 离线测请求体/响应解析/错误映射/免Key通道/接线/HITL 顺序），`npm test` 全链 188 项
- exe 实测留证（0.6.0 终版打包）：全新 profile 零凭证下 `demo-imgtest` 真实点击「测试出图」→「✓ 4898ms，出图 74KB（.jpg）」显形；核心层真实 E2E 11.7s 出 1280x720 图落盘人审；免费源下模型框 placeholder 实测为「免费直连无需模型名」

### 新增（③无API权限账号无死角）
- **发布页四步发布清单**：未接入微信 API 时，通道横幅从一句话升级为编号清单（确认成稿→复制→粘贴→后台预览发布），并明示「全程不需要任何 API 权限」「定时发布可用本页排队」
- **定时任务「提醒模式」**：未配凭证的定时任务到点不再白跑报错——自动转为「到点·等你复制发布」状态，弹系统通知指路，卡片一键「去写作页复制」；入队时即预告该行为
- **凭证短路**：未填凭证时点「立即送草稿箱/立即发布」不再发起注定失败的请求，直接显形正式方案并给「去复制/去接入」双出口；失败任务卡片新增一键改道「改用『复制富文本』」，48001 错误附人话解释
- **修复调度器真实缺陷**：`tick/runOne` 会把执行体自行转入的 `awaiting_confirm`（人工放行/提醒模式）覆盖成「完成」——即「定时到点等你放行」在真实调度路径下从未生效；现调度器尊重执行体转态，并加回归测试
- 测试门禁：新增 `test-noapi.mjs` 14 项接线断言；pipeline/scheduler 行为测试 +5 项（`npm test` 全链 104 项）
- exe 实测留证（0.6.0 终版打包·③正向）：全新 profile 零凭证下 `--gj-tab=publish` 实拍四步编号清单+「第 1 步：去复制」按钮在位；`demo-apigate` 真实点击「立即送草稿箱」→ 未发起任何 API 请求，行内显形「未填微信凭证，API 发布不可用。正式方案…」与「去写作页复制」「去接入 API」双出口（截图人审）

### 新增（④一键备份/迁移包 + 定时错过补救）
- **备份包导出**：设置页「数据与备份」一键把文章/设置/样式/素材/队列全量打成单个 JSON 备份包（含配图素材，字节级 base64），系统保存框选位置；换电脑/重装后「导入备份包」还原全部数据
- **密钥红线双向硬拒**：备份包永不含 AI/微信/生图 Key——打包时数据里混入密钥字段即拒绝生成，导入时识别到密钥字段即拒绝写入；导入后只需重填一次 Key（卡片文案明示）
- **导入安全网**：导入前主进程弹确认框（篇数/素材数/导出时间预览），确认后现有数据自动存为 `.pre-import.json` 可手工找回；损坏文件/别家 JSON/未来版本包都给人话报错且不动现有数据；素材文件名越权（`../`）直接拒绝
- **定时发布「错过显形」**：关机导致到点没跑的发布任务（迟于 10 分钟）不再开机后擅自补发——转为「错过时间·等你决定」，卡片给「仍要发布」/取消重排两个出口；仅存草稿类任务无此风险仍自动补跑；小幅延迟（≤10 分钟）视为正常执行不误伤
- 真实数据留证：对本机 15 篇文章的真实数据目录做导出→回读往返，文章/设置逐字段一致、包内 0 密钥字段
- 测试门禁：新增 `test-backup.mjs` 32 项（核心往返/密钥红线/越权文件名/全链路接线）；`test-schedule.mjs` +5 项错过行为（`npm test` 全链 141 项）

### 新增（⑤对标调研结论的 UX 超越项）
- **内置排版主题 3→8 套**：新增静墨蓝、宣纸古雅、桃夭文艺、深海科技、暮山紫，配色六色齐全、风格区分度经真实渲染验证——对标 mdnice/墨滴的模板丰富度，且全程零跳出
- **样式包导出/导入**：设置页「排版样式」可把「我的」自定义样式一键导出为 JSON、导入别人分享的样式；导入逐份校验（颜色/字号/行距越界即拒），与内置重名的跳过并说明原因，密钥字段混入直接拒收（与备份包同红线）
- **首启示例文章**：全新安装首次打开自动种一篇「3 分钟看懂稿匠」示例，演示全部 8 套主题、高亮、图槽、复制富文本用法——首启从空白页变成可照着上手的教程，明示「改它或删它都行」，仅限真正的首启（备份导入后不重复种）
- **Ctrl+S 保存**：编辑器内 Ctrl+S 直达保存，覆盖浏览器默认另存为
- **verify 去 AI 味门禁抗抖动**：段落回炉从单轮改为最多两轮（AI 味 ≤55 即停），各轮只采纳历史最优稿、终稿升分则自动回退初稿——用改动集断言守住「未标记段落零改动」的 HITL 不变量；修复 DeepSeek 输出随机性造成的两处偶发误红（≤55 冲线误判、28→29 升分误判）
- 测试门禁：新增 `test-ux.mjs` 23 项（主题数量/配色/真实渲染回退/首启种草/Ctrl+S/样式包接线全链/回炉回退不变量）；`test-backup.mjs` 扩至 42 项（`npm test` 全链 176 项）
- exe 实测留证（0.6.0 终版打包·首启正向）：全新 profile 零配置启动 3 秒截图人审——示例文章「示例：3 分钟看懂稿匠（可删）」自动种入并以青竹绿渲染预览，底部 toast 显形「欢迎用稿匠。第一步：到「设置 → AI 服务」选服务商、填入 API Key（只加密存本机）」带「去设置」按钮；状态栏如实显示「已保存 + 未自检」，无假告警；测后临时 profile 清理、零残留进程

### 修复（0.6.0 打包 exe 实测中发现）
- **首启状态栏假告警**：`#saveState` 此前在 HTML 里硬编码「未保存」，全新安装一打开就黄字告警——实际什么都没改。现改为：打开已存文章显示「已保存 + 上次保存时间」，新建文章留空，只有真实编辑才转「未保存」
- **Ctrl+S 重复绑定**：⑤ 批次在 editor.js 新加的 keydown 绑定与 workbench.js 既有快捷键表重复（按一次触发两次保存、弹两次 toast）——删去重复的一份，快捷键统一在 workbench 注册，并加「全局唯一注册」断言防回归

## [0.5.2] - 2026-09-21 — 「复制富文本」转正为正式发布方案

### 新增
- **发布页通道横幅**：进入发布页实时显示当前该走的发布路线——未配微信凭证/自检异常（40164、48001 等）时明示「复制富文本」为正式发布方式，并提供「去设置」「去写作页复制」一键直达；自检通过则确认 API 可用
- **API 发布失败一键改道**：送草稿/发布报错时，错误信息旁新增「改用『复制富文本』发布」按钮，点击跳写作页自动完成复制（配合 0.5.1 主进程剪贴板，失焦也不降级）
- 文档收口：USER-GUIDE/README 将「复制富文本」从"自动降级"表述升级为"正式发布方式"
- 设置页「单次最大输出 tokens」常显说明（0=不限制，用服务商默认），不再只靠 placeholder

## [0.5.1] - 2026-09-21 — 微信自检表单直测 + 复制富文本改主进程写剪贴板

### 新增
- **微信连通性自检支持表单直测**：填完 AppID/AppSecret 不必先「保存设置」即可点自检（留空字段回落已存凭证）；已用假凭证真实点击验证全链路：UI→主进程→微信服务器 40013→红叉显形+状态栏「API 异常」，凭证不落库

### 修复
- **「复制富文本」不再依赖窗口焦点**：旧实现走渲染层 execCommand，后台/失焦时静默只复制纯文本（实测剪贴板无 HTML Format）；改为经主进程 `clipboard.writeHTML` 直写 HTML Format。真实回读验证：剪贴板含 CF_HTML 头 + 31 处内联 style、0 个 `<style>` 块，粘贴公众号后台可保留排版

## [0.5.0] - 2026-09-21 — 稳定性加固 + 自助化（对标 OBS：不静默失败、不看代码能配置）

### 修复
- **数据丢失风险**：JSON 存储读失败不再当作"无数据"覆盖写盘；损坏文件自动备份为 `.corrupted-时间戳` 并提示（可一键打开数据目录）
- **Windows 并发写 EPERM**：同名文件并发保存串行化 + rename 重试（5 次退避），临时文件唯一命名 + fsync，杜绝崩溃点
- **调度器卡死**：启动时把中断的 running 任务恢复为 failed（可手动重试）并通知；「立即执行」改为单任务隔离运行，不再连带跑整队
- **渲染层静默失败**：队列取消/放行/立即执行、删除文章、AI 连通测试、公众号自检全部加错误显形与按钮恢复；调度器/存储异常经 `app-alert`/`store-corrupted` 事件落到 toast+系统通知

### 新增
- **崩溃保护**：编辑器 1.5s 防抖自动暂存（autosave），保存成功即清除；下次启动检测到未保存修改自动恢复并给出「丢弃恢复」选择
- **发布终态回写**：提交发布后每 60s 轮询微信 `/freepublish/get`，队列卡片显示「微信审核中…→✓ 发布成功 / ✗ 平台审核不通过」等终态并弹系统通知；立即发布也自动入队留痕跟踪（行为测试 10/10）
- **配图提示词持久化**：AI 生成/手改的画面提示词随文章保存，切页、重开不再丢失；空槽恢复占位文案
- 密钥可清除（设置页留空不再等于"不改"，后端区分 undefined/空串）；加密不可用时明示明文风险；「打开数据目录」IPC
- 预览/时间线中本地图自动转 dataURL 显示（`asset:dataUrl` 带缓存、12MB 上限、文件名防穿越）
- `npm test` 常驻行为门禁：store/scheduler 稳定性 15 项 + 发布终态轮询 10 项 + 定时发布流水线 12 项（转存/封面/放行/回写全链）

### 新增（自助化）
- **首启引导**：未配 AI Key 时启动即弹「去设置」引导；已配 Key 但缺公众号凭证时提示可跳过（复制富文本降级路径）
- **密钥自助管理**：API Key / AppSecret 旁加「清除」按钮（二次确认）；每项凭证标注「去哪拿」获取路径（DeepSeek 平台 / 公众号后台→基本配置、Secret 只显示一次、IP 白名单）
- **定时发布可选模式**：加入队列时可选「发布」或「仅存草稿」，到点行为与模式一致（有 12 项行为测试覆盖 draft 模式）
- **数据与备份卡片**：设置页一键打开数据目录（settings/articles/queue/autosave 全为 JSON，可直接备份迁移）
- **用户手册**：新增 [docs/USER-GUIDE.md](USER-GUIDE.md)——三分钟上手、凭证获取表、五页面说明、人在回路线、12 行故障排查表（401/超时/40164/48001/IP 变化/1MB 超限等）；README 快速开始接入手册链接

## [0.4.0] - 2026-09-20 — 好用起来：热点、标题、样式、Agent 参数、成稿流水线

### 新增
- **热点选题面板**：左栏「文章库/热点」切换；聚合微博/知乎/头条/抖音四大热榜（60s API，主进程代拉避 CORS，
  5 分钟缓存 + 断网回退旧缓存），一键「用此选题」新建文章并带入标题
- **AI 起标题**：按选题+正文给 7 个风格各异的候选（附风格标签与字数），浮层呈现；
  点「采用」才替换标题——纯建议，不擅自动手
- **多智能体成稿向导**：选题→调研→大纲→初稿→回炉→审核六步流水线；
  每步 AI 输出先进向导文本框，可编辑、重跑、跳过、恢复原稿，点「确认」才进入下一步；
  定稿送入编辑器后保存/发布仍是人的操作（HITL 全链路）
- **排版样式系统**：新文章默认样式可选；自定义样式增删改（重名/内置名保护），
  调色板+字号/行距/字距九项可调，编辑时实时渲染预览；写作台下拉区分「我的」与内置
- **设置页 AI 高级配置**：服务商预设（DeepSeek/Kimi/OpenAI/本地 Ollama）、全局温度滑杆、
  max tokens、六个 Agent（初稿/改写/标题/配图/调研/审核）各自模型与温度覆盖、
  「测试 AI 连通」不保存也能按当前表单直测
- 全局 `[hidden]` 显形修复；深链 `#hot`、`#demo-wiz-run-N`（向导第 N 步真实执行截图验证）

### 变更
- AIClient 支持 temperature/maxTokens 默认值与按 Agent 覆盖；`ai:agent` IPC 按 Agent 名跑单步
- 渲染入口 `renderWeChatHtml` 支持 themeOverride（自定义调色板）与排版三参数

## [0.3.0] - 2026-09-20 — 界面重设计（对标 doocs/md 与付费编辑器）

### 变更
- 外壳从"顶栏标签"改为**左侧深色图标导航栏 + 全宽工作区 + 底部状态栏**（工具心智，去网站感）
- 写作页编辑器成为主角：文档大标题头、格式工具条（加粗/斜体/标题/引用/列表/代码/链接/插图槽）、
  布局三态切换（仅编辑/分栏/仅预览，参照 doocs）、编辑预览分隔条可拖拽、字数与阅读时长实时显示
- 新增**复制富文本**：预览一键复制为带样式 HTML，直接粘贴公众号后台——48001 权限不通时的正式降级路径
- 配图页升级为"槽位→已成图"正文顺序时间线；已成图显示真实缩略图（新增 `asset:dataUrl` IPC，文件名白名单防穿越）
- 发布页新增四步流程图示；队列/配图/文章库补设计内空状态；`awaiting_confirm` 状态呼吸提示
- 文章库支持搜索过滤与悬停删除；AI 味评分环随分数变色；状态栏集中保存/IP/自检信息

### 新增
- 快捷键：Ctrl+S / Ctrl+N / Ctrl+B / Ctrl+I
- 深链 `#write|#images|#publish|#settings`；`--gj-tab` 启动参数与 `GJ_SHOT` 自截图模式（界面回归验证用）
- `js/workbench.js` UI 增强模块

## [0.2.0] - 2026-09-20 — 工程化重构

### 新增
- git 版本管理（基线提交 7b146e4 → 重构提交）
- 文档体系：README（项目总览+目录地图）、ARCHITECTURE（四层架构与数据流）、
  DECISIONS（10 条 ADR）、UI-DESIGN（人在回路五定律+视觉规范）、DEVELOPMENT、CHANGELOG
- `scripts/lint.mjs` 语法门禁（`npm run lint`）
- `scripts/verify-article.mjs` 端到端文章质量验证（`npm run verify`，19 项断言）
- 渲染层全局错误显形条（未捕获异常不再静默）
- 便携版 exe 产物（electron-builder portable，免安装）

### 变更
- 目录按 OBS 分层思想重排：`lib/ → src/core/`（引擎）、`main.cjs → src/main/` 四模块
  （index/services/pipeline/ipc + core-loader）、`renderer/ → src/ui/`（js 按功能域拆 6 文件）、
  `build/ → resources/`、打包输出 `release/ → dist/`
- 主进程启动失败不再静默退出：catch 后弹错误框并退出
- 环境变量注入密钥逻辑收敛进 services.bootstrapSecretsFromEnv

### 修复
- **致命**：渲染层 app.js 两处多余右括号导致整个脚本语法错误、界面沦为静态壳
  （用户报"无法使用"的直接原因）——已修复并被 lint 门禁永久覆盖

## [0.1.0] - 2026-09-19 — 首个完整可用版

### 新增
- Electron 单机应用：写作（文章库/Markdown 编辑/实时预览/AI 建议卡片）、
  配图（图槽协议 + AI 提示词 + 本地图落位）、发布（存草稿/立即发布/定时队列）、
  设置（AI 服务/公众号凭证/连通性自检/公网 IP/素材库）
- core 引擎：wechat.mjs（token/传图/草稿/发布/错误码表）、ai.mjs（OpenAI 兼容 +
  AI_TASKS）、md2wechat.mjs（三主题内联样式渲染/外链转角注/图槽虚线占位）、
  store.mjs（原子写）、scheduler.mjs（20s tick/重试≤3/awaiting_confirm）
- 密钥 DPAPI 加密存储；单实例锁；托盘驻留
- 微信 API 只读探测脚本 check-wechat.mjs
- PRD v0.1、交互原型 v0.2
